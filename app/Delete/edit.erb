<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>Edit <%= @delete.Id %></h1>
    <a href="<%= url_for :action => :show, :id => @delete.object %>" class="ui-btn-left" data-icon="back" data-direction="reverse">
      Cancel
    </a>
    <a href="<%= url_for :action => :delete, :id => @delete.object %>" class="ui-btn-right" data-icon="delete" data-direction="reverse">
      Delete
    </a>
  </div>

  <div data-role="content">
    <form method="POST" action="<%= url_for :action => :update %>">
      <input type="hidden" name="id" value="<%= @delete.object %>"/>
      
          <div data-role="fieldcontain">
            <label for="delete[Id]" class="fieldLabel">Id</label>
            <input type="text" id="delete[Id]" name="delete[Id]" value="<%= @delete.Id %>" <%= placeholder( "Id" ) %> />
          </div>
      
      <input type="submit" value="Update"/>
    </form>
  </div>

</div>
